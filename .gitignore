config.yaml
*.pyc
__pycache__/
.venv/
venv/
build/
dist/
*.spec
.DS_Store
secrets/
*.json

# 지점점검·상담 개인정보 데이터 — 절대 커밋 금지 (공개 저장소)
audit_results/
share_path.txt
apps_script/consult_read.gs
상담공지_엑셀/
점검표/
평가준비/
*.xlsx
# 매출점검 산출물 — 수급자 실명이 든 보고서·제외명단. 프로그램(revenue_check.py)만 커밋하고
# 결과물은 절대 올리지 않는다. 원본은 클로드코드/매출점검/<지점>/ 에 로컬 보관.
매출점검/
_8시간미만계약제외.txt
_보류제외.txt
# 매출/ 는 프로그램만 커밋. 실행하면 이 안에 지점별 실명 보고서·합본이 생기므로 전부 차단.
매출/*/
매출/매출점검_합본_*.html

# PWA 매니페스트는 커밋 필요 (docs 공개 페이지)
!docs/hq-manifest.json
!docs/rate_calc_manifest.json
# 진행추적 히스토리 — 커밋 필요(항목 상태만·개인정보 없음). *.json 에 막혀 예외 지정.
!docs/audit_history.json

# 라이브 Slack 웹훅이 들어있다 — 실수로 커밋되면 GitHub 푸시보호에 막힌다(실제로 막혔다)
apps_script_full.js
